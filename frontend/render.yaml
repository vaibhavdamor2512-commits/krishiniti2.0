services:
  - type: site
    name: krishiniti-frontend
    buildCommand: npm install && npm run build
    publishDirectory: dist
    envVars:
      - key: VITE_API_URL
        value: https://krishiniti-backend.onrender.com/api
      - key: VITE_DEMO_MODE
        value: "true"